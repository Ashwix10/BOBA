<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Boba Spin</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Full Page Gradient Background */
        body, html {
            height: 100%;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to right, #ff7e5f, #feb47b);
            overflow: hidden;
        }

        /* Container for the boba images */
        .container {
            position: relative;
            width: 300px;
            height: 300px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Boba Image Styling */
        .boba {
            position: absolute;
            width: 150px;
            height: 150px;
            animation: spin 6s infinite linear;
        }

        /* Individual Animation for Boba 1 and Boba 2 */
        .boba-1 {
            animation-delay: 0s;
        }

        .boba-2 {
            animation-delay: 3s; /* Delay for alternate spinning */
        }

        /* Keyframes for spinning animation */
        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <div class="background">
        <div class="container">
            <img src="aesthetic-boba-tea-clipart-4.png" alt="Boba 1" class="boba boba-1">
            <img src="boba2.png" alt="Boba 2" class="boba boba-2">
        </div>
    </div>
</body>
</html>
